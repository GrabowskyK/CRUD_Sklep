@model IEnumerable<CRUD_Sklep.Models.Product>
@{
    bool y = false;
}
@{
    ViewData["Title"] = "ListProducts";
}
@*
<h1>ListProducts</h1>

<p>
    <a asp-action="AddProdcuts">Create New</a>
</p>
*@
<div id="items">
    @foreach (var item in Model) {
        @if (item.Amount > 0)
        {
            y = true;
        }
        else
        {
            y = false;

        }
    <div class="wine">
            <img class="item-wine" src="https://www.euro-wino.pl/images/-------------------=przezroczyste%201.png">
        <div class="info">
            <p class="type-wine">@item.Name</p>
                <p class="type-wine">@item.Price $</p>
                <p class="type-wine">
               <form asp-controller="ShoppingCart" asp-action="AddToKoszyk" method="post">
                        @if (y == false)
                        {
                            <p color="red">Out of stock!</p>

                        }
                        else
                        {
                            <input type="number" name="amount" value="1" style="text-align:center; width: 50px;" />
                            <button type="submit" name="id" value="@item.Id">Add To Cart</button>
                        }
            </form>
        </div>
    </div>
    }
</div>
@*<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category)
            </td>
            <td>
                    @if (item.Amount > 0)
                    {
                        @Html.DisplayFor(modelItem => item.Amount)
                        y = true;
                    }
                    else
                    {
                        y = false;
                        <p color="red">Out of stock!</p>

                    }
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="DeleteProducts" asp-route-id="@item.Id">Delete</a> |

                <form asp-controller="ShoppingCart" asp-action="AddToKoszyk" method="post">
                        @if (y == false)
                        {
                            <input type="number" name="amount" value="1" disabled/>
                            
                        }
                        else
                        {
                            <input type="number" name="amount" value="1" />
                            <button type="submit" name="id" value="@item.Id">Add To Cart</button>
                        }
                            
                </form>
            </td>
        </tr>
}
    </tbody>*@
</table>
